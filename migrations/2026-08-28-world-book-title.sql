-- ============================================================
-- 2026-08-28 世界书加 title 列
-- 标题用于人类识别（写下一页的「标题」字段）；正文存 content。
-- 旧条目 title 为 NULL → 前端回退到内容第一行显示，不迁移旧数据。
-- 在 Supabase SQL Editor 手动执行（幂等，可重跑）。
-- ============================================================

ALTER TABLE world_entries ADD COLUMN IF NOT EXISTS title TEXT;
