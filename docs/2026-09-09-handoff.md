# 交接文档 · 2026-09-09（写给下一个接手的人 / 模型）

这份文档的存在理由：09-08~09-09 这一轮做了两件大事 —— **自洽性审计**（找出「代码写对了但没接上」的静默故障）和 **server.js 分区**。过程中攒的判断和踩过的坑，如果只留在会话里就没了，下一个人会把同样的坑重踩一遍。

读法：先读「§0 现状」和「§4 踩过的坑」，这两节省时间最多。要动手再看对应章节。

---

## §0 现状快照（2026-09-09 收工时）

| | 状态 |
|---|---|
| 后端 `shenyan-backend` | HEAD 见 git log（2026-09-09 收工时为 `5f21264`），未推送 0，工作区干净（除 `node_modules/.package-lock.json` 常年 M） |
| 前端 `angel-garden-diary` | 有别的会话在并行改（色卡等），本轮只动过 `整体框架·当前状态净本.md` |
| 线上 | 已部署最新后端；音乐室扫码登录已修好并验证 |
| 测试 | `npm test` **52/52** |
| `server.js` | **9619 → 7509 行**（本轮搬走 2110 行） |

**已抽出的模块 —— 分区第 2 步 6/6 完成**

```
lib/    tools-schema.js  cache-control.js  time.js  share-parse.js
        deepseek-json.js  llm.js
routes/ backup.js  music.js  calendar.js  share.js  moments.js  auth.js
```

**测试构成**：`sse-parser` / `sse-replay` / `routes`（84 条路由快照 + 遮蔽检测）/
`lib-time` / `lib-cache-control` / `lib-share-parse` / `lib-tools-schema` / **`auth-gate`**。
最后一条是登录门不变式，专门锁「未登录访问受保护端点必须 401」，
`npm run test:auth` 单跑。**改鉴权相关的任何东西之前先跑它。**

**⚠️ 部署纪律（本轮踩过）**：面板「重启」只重读环境变量、**复用旧镜像，代码不更新**；
要更新代码必须真部署（前端仓库根目录 `bash _deploy.sh`，不再需要任何环境变量）。
Zeabur 构建期间 `deployment list` 显示 BUILDING、线上跑旧实例，**这是正常中间态**。

> ⏱️ **耗时已核实（2026-09-10 查部署记录）**：一次部署 **60–90 秒**，不是早先记的 25–30 分钟。
> 08-30 那晚 4 次分别 60s / 86s / 76s / 67s。**别再把部署当成「要等半小时」的事来排期。**
>
> ☠️ **裸跑 `zeabur` 就等于 `deploy`。** 在哪个目录跑就打包哪个目录 —— 2026-09-10 在
> `C:\Users\hbyll` 裸跑了一次，它当场开始打包主目录（幸好撞上符号链接报错停下）。
> **永远带显式子命令**（`deployment list` / `service ls` …）。
>
> 🚨 **「只推后端」这件事不存在。** 本仓库的 Dockerfile 是 `COPY . .`，而它同时托管前端
> （server.js:51 `express.static(__dirname/public)`，同域名出）。所以一次 `zeabur deploy`
> 传的是**整个工作区** —— `public/` 里躺着哪个前端版本，线上就变成哪个版本。
>
> 2026-09-10 就栽在这上面：查部署列表时用了 `head -8`，**只看到最旧的 8 行**，据此误判
> 「09-09 之后没部署过」，于是把 `public/index.html` 钉回已提交版才部署 —— 实际效果是
> **把线上前端倒退了一版**（opus 早在 21:45 就部署过它的前端了）。线上旧前端跑了约 4 分钟，
> opus 22:38 自己重新部署才复原。
>
> **两条教训**：①看部署记录要看**最新**几条，别 `head`（这仓库的历史短、ASCII 排序下
> 最旧的在最上面）；②有别人在同一服务上平行部署时，「后端改动单独上线」做不到——
> **要动就先跟对方对时间**。

---

## §1 分区第 2 步：方法（照抄即可）

6 个域已全部完成，方法固定下来了。**每搬一块都按这五步，不要跳。**

### 步骤

1. **划边界**：找区块起止行（用 `// ===== ` 标记 + 下一个非本域路由定位）。
2. **依赖分析**（关键，别省）：用 `scripts/audit/dep.cjs`（用法 `node scripts/audit/dep.cjs <起始行> <结束行>`，已入库）看三件事：
   - 区内定义了什么
   - **区内符号被区外引用**（= 搬走会断，必须由工厂交出去）
   - 区内用到的区外符号（= 要注入的依赖）
3. **写模块**：统一形态是**工厂 + 显式依赖注入**：
   ```js
   module.exports = function createXxx({ supabase, ... }) {
     if (!supabase) throw new Error('createXxx: 缺少 supabase 依赖');
     const router = express.Router();
     ... router.get('/子路径', ...)
     return router;           // 或 { router, 别的要交出去的能力 }
   };
   ```
   **绝不用 `require('../server')` 反向引用** —— 那是循环依赖，CommonJS 不报错、只给
   `undefined`，运行到那行才 TypeError。本项目今天已经踩过三次同类静默故障。
4. **回填 server.js**：原位置换成 `app.use('/api/前缀', createXxx({...}))`，require 加到文件顶部那一串后面。
5. **验证**（三条缺一不可）：
   - `node -c` 两个文件
   - **`npm test` 52/52** —— 路由快照会逐条比对 84 条路由的「方法 + 完整路径 + 注册顺序」
   - `grep` 搬走的每个符号在 server.js 里**零残留**（有残留 = ReferenceError，但可能要到运行时才炸）

### 搬运脚本的写法
用 Node 脚本做机械替换（`app.METHOD('/api/x/y')` → `router.METHOD('/y')`），**每步下断言**：
路由数必须等于预期、改写数必须等于路由数、块内不许混进别的东西 —— 任一不满足就 `process.exit(1)` 整体中止。
搬运脚本本身是一次性的、没入库；照上面的描述重写即可，不复杂。常驻工具在 `scripts/audit/`。

⚠️ **不要用 bash heredoc 写这些脚本**：本轮多次遇到 heredoc 吃掉反斜杠（`\\b` 变成退格符）导致正则失效。用文件写入工具直接写 `.cjs`。

### 路由护栏（`test/routes.test.cjs`）
2026-09-09 从「正则扫源码」改成**运行时遍历 Express 路由栈**。原因：搬到 `app.use` 挂载后，源码正则看不见那些路由 —— backup 刚搬完两条路由就从快照里消失了。
坑：Express 5 不再把挂载前缀暴露在 `layer.path` / `layer.regexp` 上（藏进 `matchers` 闭包），所以测试里先给 `express.application.use` 打补丁，注册那一刻把前缀记到 router 函数上。

**快照是行为不变式。搬运时它应当纹丝不动 —— 如果变了，先怀疑自己搬错了，不要急着 `--update`。**

---

## §2 分区第 2 步：**已全部完成**（以下为过程记录，含两块难点是怎么处理的）

> 2026-09-09 晚：`moments` 与 `auth` 也搬完了，第 2 步 6/6。下面两小节保留原来的难点分析 + 补上实际做法，因为**第 3 步会遇到同类问题**（反向依赖、顺序变化、中间件）。

### 实际做法补记

**moments** —— 三个难点各有对策：
1. 四个前缀 → 工厂交出**四个 router** 分别挂载。
2. 是枢纽不是叶子 → 工厂返回对象，把 5 个「被外部调用的能力」一并交出
   （`processDueReplies` / `processDueCommentReplies` / `ensureMomentsBucket` /
   `storeChatKeepsake` / `postAngelMoment`），同 calendar 的模式。
3. **顺序会变**（12 条位置变化）→ 快照必然不一致，所以**不能直接 `--update`**。
   改用三条独立证据：① 排序后集合比对（84 条一条不多一条不少）② 位置变化明细
   （正好是预期那 12 条，无第三方被牵动）③ 重跑遮蔽检测（0 处）。三条都过才重生成快照。

**auth** —— **先立护栏再动刀**：
1. 先写 `test/auth-gate.test.cjs`，并**往中间件里注入 `return next()` 自证它会红**
   （5 条中 3 条变红）。测不出失败的安全测试等于没有。
2. 然后才搬。工厂交出 `{ router, requireAuth }`，server.js 里保持严格顺序：
   `app.use('/api/auth', router)` **必须在** `app.use(requireAuth)` 之前，
   两者都在其余所有路由之前。这两行上方写了警告注释。
3. 搬完：护栏 5/5、路由表 84 条**逐条一致含顺序**、7 个符号零残留。

### 又踩了一次的坑（第二次了，写进 §4 了）
搬 moments 时脚本按 `postAngelMoment(` 找外部调用点，**一处都没有** ——
因为带括号的调用全在被搬走的块里，唯一的外部引用是 `module.exports` 里的**裸标识符**。
和前一天裁 `callDeepSeek` 的 import 是同一个坑。

### 顺带修好的一个工具假象
`dump-routes.cjs` 比对时报「`/api/moments` 丢失、`/api/moments/` 新增」。
起本地服务实测：两者都进 handler，对照组 `/api/definitely-not-a-route` 才 404 ——
说明 Express 对挂载路由的尾斜杠等价，差异是拼接（`'/api/moments' + '/'`）造出来的。
已在导出器里归一化。
（实测时还发现：`dotenv` 会从 cwd 读到真 `.env`，`SITE_PASSWORD` 一设**全部 401**、
对照组失效 —— 探针里要显式 `process.env.SITE_PASSWORD = ''` 才测得出来。）

---

## §2-旧 当时对这两块的难点分析（保留备查）

### A. `routes/moments.js` —— 朋友圈 + 相册

**为什么难**：
1. **横跨四个前缀**：`/api/moments`、`/api/keepsakes`、`/api/month-summary`、`/api/angel/moments`。单个 `app.use` 挂不了，要么一个模块交出多个 router 分别挂载，要么挂在 `/api` 上（不推荐，会和别的 `/api/*` 抢顺序）。
2. **它不是叶子节点，是枢纽**。目前仍有 **5 个符号往外漏**：
   - `processDueReplies` / `processDueCommentReplies` —— 被 `listen()` 里 15 分钟的 `setInterval` 调
   - `ensureMomentsBucket` —— 被启动时调
   - `storeChatKeepsake` —— 被聊天主链路（`handleChat`）调
   - `postAngelMoment` —— 2 处
   这些是「被外部调用的能力」而非「通用工具」，**应由工厂一并交出**，形态同 `routes/calendar.js` 的 `{ router, buildCalendarBlock }`。
3. **顺序问题**：日历的挂载点目前夹在 moments 路由中间，把它们劈成前后两段。全部搬到一个挂载点会**改变路由注册顺序** → 快照会变。
   处理办法：先用 `scripts/audit/dump-routes.cjs` 导出前后两份，做**排序后的集合比对**确认「路由一条不多一条不少」+ 跑遮蔽检测确认无新冲突，**再** `--update` 快照，并在 commit 里写清为什么顺序变了、为什么安全（不同前缀不相互遮蔽）。

**已经先做掉的准备**：通用 LLM 调用（`callDeepSeek` / `callOpenRouter` / `callReplyModel` / `callVisionModel` / `parseJsonLoose` / `randomDelay`）已抽到 `lib/llm.js`，它们原本住在朋友圈区但不属于那个域。

### B. `routes/auth.js` —— ⛔ 最敏感，留给人做

登录门的**中间件注册顺序决定了哪些请求需要登录**：
`express.static` → `/api/auth/*` 放行 → 鉴权中间件 → 其余所有路由。
搬错位置 = 把门拆了，而且**不会报错**（所有请求照常 200，只是不再需要登录）。
路由快照测不出这个（它只看路由表，不看中间件顺序）。
真要动，必须补一条「未登录访问受保护端点应得 401」的测试再动。

---

## §2-新 下一步：分区第 3 步（心脏地带，最后再碰）

第 2 步搬的都是**业务域**（路由 + 该域自己的辅助函数）。第 3 步要碰的是**核心链路**，
它们互相咬得最紧，而且没有「路由快照」这种现成的不变式可以护航：

| 目标 | 规模 | 难点 |
|---|---|---|
| `lib/context/` | `buildModelContext` 单函数约 480 行 + 检索层 + 注意力 | 上下文组装是热路径，改错了症状是「他变笨了」而不是报错 |
| `lib/memory/` | 记忆编辑者约 577 行 | 写门控 + 差分写回 + Ombre 契约 |
| `lib/wake/` | keepalive + reflection 约 985 行 | pick_intent 决策 + 安全阀 |

**开工前先补不变式**，就像 auth 之前先补 auth-gate 那样。建议顺序：
1. 先给 `buildModelContext` 补「给定固定输入 → 组装出的 messages 结构稳定」的快照测试
   （可以拿 `request_stats` 里真实的 diagnostics 字段当断言口径：frozen_turns /
   live_turns / summary_present / attention_injected 这些数），
2. 有了这条再动刀。没有它，搬完只能靠肉眼，等于赌。

也可以**先不做第 3 步** —— 7509 行虽然还长，但已经不是「23 个业务域挤在一起」了。
剩下的都是同一件事的不同层（组装上下文 → 调模型 → 落记忆），
耦合是**本质的**而不是历史遗留。硬拆可能反而更难读。这个判断留给她。

## §3 自洽性审计：已验 / 未验

方法：**不看代码写没写，只找「它真的在跑」的证据**。四条查法，本轮全部命中过：
① 表里有没有行 ② 计数分布是不是恒为 0 ③ 闸是不是永远同一边 ④ 拿真实值去 `.eq()` 探列类型（只读就能验出类型错配）。

### ✅ 已验证健康（这些 ✅ 现在是有证据的）
- `mirrorDaySweep` 定时清扫：自续排链完整（成功与 catch 两条路都重新排程）
- **记忆写入链**：`request_stats.memory_degraded` 672 行里 0 条降级；`bucket_id` 缺失的 36 条全在 08-15 之前，是 Ombre 契约那个 bug 修好前的疤，08-16 起 100% 正常
- **缓存锚定**：单请求命中率**中位数 99.5%**（⚠️ 别用日聚合看，它按 token 加权会严重低估）

### ❌ 本轮修掉的五个真 bug
1. 8 组 settings 配置里 **4 组静默失效**（迁移没跑 + `live_max_tokens` 压根没有迁移）
2. 念头池生命周期清扫**从建成起一次没跑过**（`settled` 恒 0）
3. **表达资格隔离（P0 边界协议）空转十天** —— `prompt_injections.session_id` 建成 uuid 而 `sessions.id` 是整数
4. `settings.netease_cookie` 这个列**从来没建过** → 网易云登录存不下来
5. 存的是**原始 Set-Cookie 整串**（属性没剥）→ 网易云不认 → 登录几秒就掉
6. （附带）`lib/time.js` `memoryMdLabel` 往年记忆读成「2025年**年**8月25日」，2027 元旦会全面爆发

### ⬜ 还没查的（净本 §4 里剩下的）
- **provenance**（动态块挂 `prov`，日志 `prov=[...]` 审计）
- **声音渲染层**（`voiceifyMemory`：memory 桶中性正文被想起时改第一人称）
- **关系边**（`memory_relations`，1~2 hop 因果邻居，已接 attention）
- **感知天线**（天气 / device）
- **保温 Keeper**：机制没问题，但**至今 0 次触发** —— 因为 `cacheWarmStore` 是进程内 Map、只在真实请求后才有内容，而 08-31 后没有对话。要等她重新聊天才能验。

### ⏳ 卡在「需要真实对话」才能验的三件
她 2026-08-31 之后就没和沈晏说过话（吵架了）。以下三样的验证钥匙在她手里：
1. 表达资格隔离修好了，但 `prompt_injections` 台账要有对话才开始进货
2. 记忆写入「准入三刀」的验收口径（feel 部署后只有 3 条样本，n 太小判不了）
3. 念头池现在是干净的空（67 条旧念头已一次性 settled），等残留喂新念头

---

## §4 踩过的坑（读这节最省时间）

### 关于这个项目
1. **PostgREST 的 `.select('a, b, c')` 只要一个列不存在就整条查询报错** → 配上 `if (error) return DEFAULTS` 就是**整组配置静默退默认**。已加 `warnConfigFallback` 一次性告警。
2. **`console.warn` 每轮都喊 = 等于没喊。** 注入台账写失败喊了十天没人看见。改用 `warnOnce(key, msg)`（server.js 里有）。
3. **`sessions.id` 是整数不是 uuid。** 新建表引用它时别写成 `uuid`。
4. **后端 `SUPABASE_KEY` 是 `sb_secret_` 开头的 secret key（绕过 RLS），前端不直连 Supabase。** 所以给表开 RLS 对后端零影响 —— 但如果哪天换成 publishable/anon key，开 RLS 会当场锁死应用。
5. **迁移不会自动跑**（`.env` 里没有 `PG_CONN_STRING`，`npm run migrate` 用不了），只能去 Supabase SQL Editor 手动贴。
6. **`settings` 表没有建表脚本**（面板手工建的），所以「迁移文件里没有某列」≠「该列不存在」，必须查库。

### 关于工具和方法
7. **不要用 bash heredoc 写含正则的 Node 脚本** —— 反斜杠会被吃掉（`\\b` → 退格符），脚本静默失效或报奇怪的语法错。用文件写入工具。
8. **`awk` 的正则里 `\<` `\(` 会告警并炸**，判断标识符边界用 Node 而不是 awk。
9. **判断「某符号还用不用」必须连裸引用一起数。** 本轮按 `grep 'callDeepSeek('` 裁剪 import，结果它是**作为依赖注入**传出去的裸标识符 —— 当场 ReferenceError。
10. **依赖分析脚本的计数要用 grep 调用点复核**，别直接信（本轮它报「区外 8 处」，实际是 1 处注入）。
11. **看命中率别用日聚合** —— token 加权会让一次冷启动全写盖过几十次高命中。要看**单请求分布的中位数**。
12. **「某机制在某个日期之后停了」，先问有没有输入**，别急着归因到那天的部署。（念头池 08-31 后没新增，我一度怀疑是那天的「入池收紧」，实际是她没聊天。）

---

## §5 悬而未决 / 需要她拍板

1. **表达资格隔离的历史数据无法追溯修复**：台账空了十天，此前 64 张镜子卡（44 张 verified）当时都没被真正筛过。若已升进 `personality_claim`，事后分不出哪些是回响。**要不要重审石头，是她的决定。**
2. **`node_modules` 有 1178 个文件被 git 跟踪**（`.gitignore` 里明明写了 `node_modules/`，是当年先 add 后 ignore 留下的）。动它要先确认不影响 Zeabur 构建。
3. **保温 Keeper 的 6 小时上限**：想覆盖更长空档就得改，代价是持续烧钱。取舍不是缺陷。
4. **keepalive 目前是关着的**（`gate: disabled`）。念头池清扫已与唤醒解耦，所以关着不再攒垃圾；要不要重开是她的事。
5. **她提过想把 Claude Code 放到自己 VPS 上接前端**。重点在权限边界（等于把能读写全部数据的凭证长期放在联网机器上），不在能不能跑通 —— 尤其 Zeabur 刚出过资安事件。

---

## §6 可复用的工具（已入库 `scripts/audit/`，路径可移植）

| 脚本 | 用途 |
|---|---|
| `dep.cjs <起始行> <结束行>` | 区间依赖分析：区内定义 / 外漏符号 / 需注入的依赖 |
| `dump-routes.cjs <输出文件>` | 遍历 Express 运行时路由栈导出完整路由表（含挂载前缀） |
| `scan-reads.cjs` | 扫全仓 `.select('a,b,c')` 读的列，逐个探表是否存在 |
| `scan-writes.cjs` | 扫全仓 `.insert/.update` 写的列，同上（⚠️ 会误报：`.from()` 与 `.insert()` 之间可能跨表；`storage.from()` 是存储桶不是表 —— 每条都要手工核对） |

后两个是**发现三个真 bug 的主力**，新增表或列之后值得重跑。

---

## §7 审计剩下的四条机制（✅ 2026-09-10 已做完 —— 结果见文末 §8）

为什么挑这个：今天审计的产出率明显高于分区 —— 五个真 bug，其中一个是文档自己标
「风险最高」的 P0 协议**空转了十天**。分区是让代码好看，审计是让你知道它**是不是真的在跑**。

### 要查的四条（净本 §4 里还剩的）

| 机制 | 在哪 | 怎么查「它真的在跑」 |
|---|---|---|
| **provenance** | `buildModelContext` 里给动态块挂 `prov`，日志打 `prov=[...]` | 线上日志里搜 `prov=` 有没有出现过；`prov` 的 8 种来源是不是都出现过，还是某几种恒缺 |
| **声音渲染层** | `voiceifyMemory`（memory 桶中性正文被想起时改第一人称） | 它走 `callDeepSeekJson(..., 'voiceify')`；线上日志搜 `[voiceify]`；再看 `memory_topics` 里被注入过的正文是不是真的改了口吻 |
| **关系边** | `getRelationNeighbors`（1~2 hop 因果邻居，已接 attention） | `memory_relations` 表有多少行？`request_stats.attention_hits` 里有没有来自关系扩展的？边是不是只有单向 |
| **感知天线** | 天气 / device | `request_stats` 有没有相应注入记录；天气「只在首句/隔久回来时注入」这条纪律有没有被遵守 |

### 四条通用查法（今天全部命中过）
1. **表里有没有行** —— 空表 = 上游从没写进来过（表达资格隔离就是这么发现的）
2. **计数分布是不是恒为 0** —— `settled 0 / graduated 0` 揭穿了念头池清扫从没跑过
3. **闸是不是永远同一边** —— 某个 gate 的两个分支只走过一边，另一边就是死代码
4. **拿真实值去 `.eq()` 探列类型** —— 只读就能验出类型错配（`session_id` 建成 uuid 那个）

现成工具在 `scripts/audit/`：`scan-reads.cjs` / `scan-writes.cjs` 扫「代码读写的列表里有没有」，
新增表或列之后值得重跑（它们是今天发现三个真 bug 的主力）。

### ⛔ 纪律（外包时尤其要写进任务单）
- **只查不修。** 发现问题写进报告交回人判断，不要自行改代码 ——
  今天 DeepSeek 交回的三条里，**有两条是任务单写错了而代码是对的**
  （`lenientJsonParse` 抛异常是承重的控制流，改成返回 null 反而破坏调用方）。
  它没照着错误描述改代码，这个判断力比完成任务本身值钱。
- **探针一律只读。** 需要写库的诊断先问人。
- **别碰 `.env` 和线上环境变量。** 读它们会把密钥落到磁盘或打进日志
  （09-08 就有会话用交互模式把全部变量连值打屏幕上，导致 DeepSeek key 必须轮换）。

### ⏳ 有三样现在查不了，别浪费时间
她 2026-08-31 之后没和沈晏说过话。所以「表达资格隔离台账进货」「记忆准入三刀验收」
「念头池新念头」这三样**必须等她重新聊天**才有数据。看到相关表是空的，
先确认有没有输入，**别急着归因成 bug**（今天我就在念头池上推错过一次）。

---

## §8 2026-09-10 续：四条机制审计已做完（一个能验、三个验不了、外加一个真窟窿）

§7 把四条列为「下一件事」，2026-09-10 做完了。探针入库：`scripts/audit/probe-mechanisms.cjs`
（**全程只读**，只有 `.select()`，不打印密钥值）。测试仍 52/52。

### 结论表

| 机制 | 结果 | 一句话证据 |
|---|---|---|
| **关系边** | ✅ **确认在跑** | `attention_hits=4` 的 5 行 = 2 主召回 + 2 联想席 |
| 世界书 / 天气 / device | ✅ **不是坏的，是没输入** | 唯一一次注入精确对上含关键词的那句 |
| provenance | ⬜ 仍验不了 | `prompt_injections` 0 行（无输入，非 bug） |
| 声音渲染层 | ⛔ **设计上不可审计** | 全链路不留任何持久痕迹 |

### 关系边 —— 本周唯一拿到硬证据的

不是看代码，是**算出来的**：配置 `attention_k=2` 让主召回循环卡死在 2 条
（`if (hits.length >= cfg.k) break`），所以 `attention_hits > 2` 的部分**只可能来自联想席**。
分布里 **5 行 = 4**（2 主召回 + `assoc_seats=2` 两个联想席填满），时间全落在
`08-28 23:09 → 08-29 00:43`，正好在关系边 08-26 上线之后。

数据接口也是通的：40 条边、58 个不同端点，**全部**能在 `memory_topics.topic` 里找到，
topic 名零重复 → **不是死代码**。（这个查法是方法④的变体：拿真实值去探能不能接上，
和 `prompt_injections.session_id` 建成 uuid 那次是同一个形状。）

### 世界书 / 感知天线 —— 差一点误判成 bug

4 条 `world_entries` 全部 `enabled=true`、keywords 齐全（小窝/勿忘我、花园/记忆、语音/打电话、宝贝/程芥），
但 672 行里只注入 1 次。**先去查了输入**：她 08-26 起 127 条消息里只有 3 条含这些词，
而唯一那次注入（`08-31 11:54:22, mode=深入, kind=setting`）精确对应 11:53 那条含「记忆」的消息，
前后无关键词的轮次全是 `false`。**逐条对得上，是健康的相关性。** §7 的纪律 #12 又救了一次。

### 声音渲染层 —— 这条不是「还没验」，是「验不了」

代码事实：渲染成功**不打日志**、失败**静默降级回原文**、结果只存在进程内 `voiceCache`，
不落任何表。所以「它到底有没有在改口吻」这个问题**未来也查不出来**。
这是个真缺陷（可观测性缺口），不是待办 —— 本次**没动代码**，留给下一次决定。

### ⚠️ 顺带撞到的一个真窟窿：`request_stats` 有整段盲区

本来在核世界书注入率，顺手对了「他答 N 条 ↔ request_stats N 行」，发现账本**有整段是瞎的**。
其余每一天都是 0 差精确对上，所以不是噪声。

> **⏰ 时间口径**：DB 里的 `created_at` 一律是 **UTC**；git 提交时间和 Zeabur 部署记录是 **+0800**。
> 两者差 8 小时 —— §8 初稿就是在这里栽的（见下面「已钉死」）。

| 日期 | 相邻两行的账本空白 | 其中**真有对话**的时段（UTC） | 丢的轮数 |
|---|---|---|---|
| 08-08 | 三处，最长 18h | —（洞内那条回复就是洞后那行自己的） | 0 |
| 08-15 | `08-11 15:53 → 08-15 19:07` | `17:14:22 → 19:07:55` | 11 |
| **08-30** | `08-30 11:45 → 20:33` | **`18:24:01 → 20:33:55`** | **56** |

**数法**：洞内 assistant 回复数 **− 1** —— 最后那条是洞后那一行自己的回复（行在回复之后才写）。
不减这一条，每个正常间隔都会被误记成「丢 1 轮」。合计 **67 轮**没进表。

**⚠️「相邻两行的间隔」≠「冻结时长」。** 中间可能整夜没人说话。08-30 那段看着 8.8 小时，
**真正被吞掉的对话只有 2 小时 10 分**（本地 `02:24 → 04:33`）。08-15 那段同理。

08-30 丢的那段抽过内容核对：**是连贯的真对话**（session 497，id 连号 1491–1606），
不是回填的消息。

**根因 = 已经修过的那个。** 08-31 04:08 的提交 `cd75a04 fix(stats+cache): request_stats 列缺失致 stats 冻结`
——提交信息自己就写着。机制是本项目的结构性问题：**代码先部署、迁移不自动跑**，
新诊断列一写，PostgREST 对未知列**整行 400**，`recordRequestStat` 只打一行 warn，账本整段冻结。

**最硬的一条证据**：`live_anchor_turn` / `live_collapsed` / `live_tokens_est` 三列的
**首次非空时刻 = `2026-08-30 20:33:57`，与解冻是同一分钟**。也就是说：补跑迁移的那一刻，
列才出现、行才恢复 —— 冻结的成因就写在这三列上。

**为什么要记下来（对成本账本的修正）**：这 67 轮真实对话走的是 OpenRouter `anthropic/*`，
**会进 OR 账单但没进 request_stats**。`cost-ledger` 那条把「OR $29.19 vs 明细 $20.03」的
$9.16 缺口全归因给「调试期直打 OR」——**其中一部分其实是这里**。

**✅ 08-30 的 breaker 已钉死（2026-09-10 查 Zeabur 部署记录 + 逐版本 diff）**

初稿写的「盲区起点 11:45 早于当天最早提交 19:45，触发它的部署没有对应 commit」是**时区错误**：
`11:45:26Z` 就是本地 `19:45:26`，而 `f05766c` 提交于本地 `19:45:52` —— 差 26 秒，同一分钟。

真正链路（本地 +0800）：

| 时间 | 事件 |
|---|---|
| 08-30 19:45:26 | 最后一条正常行（id 645） |
| 08-30 20:27:50 | 部署（含 f05766c）—— **不是它**，insert 逐字未变 |
| 08-30 20:58:17 | 部署（含 4ab6f4e）—— 同样没碰 insert |
| 08-30 **21:31:20 → 21:32:47** | 部署上线（`6a9430a837a5d65585783e8b`，76s） |
| 08-30 **21:32:35** | **`6e26306`** 提交 —— 就在这台实例上线前 12 秒 |
| — | ☠️ 账本冻结 |
| 08-31 02:23:44 | 冻结后第一条对话 |
| 08-31 04:21:54 → **04:23:42** | `cd75a04` 的部署上线（67s） |
| 08-31 **04:33:57** | 账本恢复（id 646，`live_*` 首次非空） |

**为什么是 21:31 那次部署**：Zeabur CLI 部署上传的是**工作区**、不是 commit
（所以部署记录的 `commitSHA` 一直是空的）。21:32:35 刚写好的代码，正好落在这台 21:32:47
上线的实例里。

**判定 `6e26306` 是 breaker 的硬证据**：把三个版本的 `recordRequestStat` 插入段抽出来逐字比 ——
`58342d6` 与 `f05766c` **完全相同**，`6e26306` 恰好多了三行
（`live_anchor_turn` / `live_collapsed` / `live_tokens_est`）。嫌疑窗口内只此一处改动。
迁移脚本 `2026-08-31-request-stats-columns.sql` 的注释里她自己记的也是这个 commit。

**冻结总时长**：本地 21:32:47 → 04:33:57 共 **7 小时**，但其中只有 `02:23:44–04:33:55`
这段在说话 → **56 轮**。解冻要两样一起到位：`cd75a04` 的部署（04:23:42 上线）+ 手动跑迁移
—— 恢复行 id 646 带 `live_*` 非空，说明迁移是在 04:23–04:33 之间跑的。

**只读探针到不了这一步**：CLI 的 `deployment list` 只回最近 5 条、没有分页参数。
工具已入库：`scripts/audit/zeabur-deployments.cjs`（走 GraphQL，token 从 CLI 配置读、不打印）。
注意 `searchRuntimeLogs` 要 Pro 套餐，Free 版只能用 `runtimeLogs`，而它保留期短 ——
08-30 的日志已经取不到了，**所以这次是靠部署时间 + diff 定的案，不是靠当时的报错原文**。

### ✅ 同一个洞已堵（2026-09-10 落码，**未部署**）

原来的 `baseRow` 降级**兜不住**，三条：①它自己仍带着 `keepalive_action` / `keepalive_meta` /
`memory_degraded` 三个较新的列——缺的若是这三个之一，两次插入都失败、**整行照样丢**；
②它把 `residue_mode` 丢了（降级即掉字段）；③失败信号只有一行 `console.warn`——正是教训 #2
「每轮都喊 = 等于没喊」，`prompt_injections` 空转十天就是这么发生的。

**改法**：PostgREST 报错**带列名**（实测 `column request_stats.xxx does not exist`），
所以从报错里抠出缺的那一列，**剥掉重试**——缺什么只丢什么，账本不再整段瞎。
新增 `lib/stats-row.js`（`missingColumnFromError` / `stripColumn` / `pickColumns` /
`insertRowResilient`），逻辑住 lib 里是为了能被单测拿假 insert 跑通，而不是只靠肉眼。
`recordRequestStat` 改成调它；失败与降级都收进 `warnOnce`（降级按缺的列集合分键）。

**认不出来的报错怎么办**：剥不动就退到「只写建表起就在的核心列」，尽量别丢整行；
连核心列都写不进才 `warnOnce` 喊一次「账本正在丢行」。**认不出列名时绝不猜**
（猜错会剥掉不该剥的列），这条有单测守着。

**护栏自证过**（照 auth-gate 的办法）：把 `missingColumnFromError` 临时弄瞎 → 10 条里
**4 条变红**（含 08-30 那条回归用例），还原后 62/62 全绿。
新增 `test/lib-stats-row.test.cjs`（10 条），`npm test` 从 52 → **62**。

⚠️ **仍未验证的一点**：单测用的是**假 insert**，实测的又是 **select** 的报错文案。
Supabase JS 在某些版本上 **insert** 会返回另一种（`Could not find the 'x' column of
'request_stats' in the schema cache`）——两种都写了正则，但**真的 insert 是什么文案，
只有线上真实插入能确认**。所以：**部署后要盯 `⚠️ request_stats 写入降级` 这条日志**，
看它是不是在正常工作（而不是悄默声地走了核心列模式）。

**部署注意**：这是热路径上的写库逻辑，且 `request_stats` 是成本账本的唯一来源。
建议部署后第一次对话就去看一眼有没有新行、`usage_raw` 是否完整。

---

## §9 2026-09-10 续：分区第 3 步开工（第一片 + 第二片已落地）

第 3 步（核心链路）已开工，走的是 §2-新 里说的顺序：**先补不变式，再动刀**。
两片都已提交、`npm test` **91/91**（含 84 条路由快照）、**未部署**。

| 片 | 内容 | server.js | 不变式 |
|---|---|---|---|
| 一 | `lib/context/select.js` —— 纯选择器（topicHits / extractNgrams / isExactWord / 世界书 mode×kind 矩阵） | — | `test/fixtures/context-select.baseline.json` |
| 二 | `lib/context/retrieval.js` —— 注意力分配 + 关系扩展 + 世界书检索（340 行） | 7389 → **7062** | `test/fixtures/context-retrieval.baseline.json` |

### 方法上的三处升级（都比上一轮硬）

1. **自由变量用 AST 算，不靠硬编码白名单**。`scripts/audit/free-vars.cjs`（acorn，devDependency；
   生产 `npm install --omit=dev` 进不去）。检索层报「区内定义 17 个，需注入 12 个」——
   工厂签名就是从这个数里减掉 lib 侧那 6 个得来的，不是猜的。
2. **基线工具支持 IO 桩**。`scripts/audit/baseline-dump.cjs` 加了 `spec.deps`（往沙箱注入全局）、
   `adapt`（把「rev 的裸函数集」和「模块的工厂」归一成同一形状）、async 调用、`baseRev`。
   ⚠️ `adapt` 第一版只被当 flag 判断、**从没真调用过**，是搬检索层时才发现的 —— 已修。
3. **冻钟**。见下。

### ⚠️ 检索层的行为一半在 IO 序列里 —— 这是和第一片最大的不同

纯函数基线证明不了「查询没变」。所以第二片的 39 组向量每组比五样东西：
返回值 / **假 supabase 记录的查询序列（含 select 的列清单）** / warnConfigFallback 调用 /
**voiceify 真的打了几次模型** / console 输出 / 召回计数器。

**冻钟这件事踩了才知道**：检索层有三处读时钟 —— 打分带 30 天半衰（`score` 是浮点，
差一毫秒就不逐位相同）、回声压制按「多久以前」判、召回日志按天切（`recallDaily.date`）。
不冻钟，同一份向量隔一分钟跑两遍都不逐字节相同，基线就成了跟时间赛跑。
⚠️ **`Date.now()` 和 `new Date()` 是两个口子**，天切走的是后者，只补前者不够。
基线里第一条断言就是「同一份向量跑两遍必须逐字节相同」。

假 supabase / 冻钟 / 夹具现已抽去 `scripts/audit/testkit.cjs`，基线 spec 与单元测试共用
（抽完复查基线逐字节未变 —— 抽的是壳，不是行为）。

### 需要人过目的四条

**① 模块多导出两个名字（需要点头）**。`lib/context/retrieval.js` 除了 server.js 消费的
`getAttentionMaterial` / `getRelationNeighbors` / `retrieveWorld`，还导出 `getAttentionConfig`
和 `recallDaily`。理由：前者是 09-09 审计里静默失效的那层，不交出来基线压不住「整数列给
小数 / 给负数 / 给 0」这些逐条守卫；后者搬迁前唯一出口是那条**跨天才打**的 📊 [recall] 日志，
基线够不着。两者都是纯读、零行为改动。**这是本次唯一一处非逐字的增补。**
不想要就删掉那两项，同时删掉 spec 里对应的 5 组 `cfg_*` 用例。

**② server.js 里多了 5 个死引入（没裁）**。搬走检索层之后，
`require('./lib/context/select')` 里的 `extractNgrams` / `isExactWord` / `isStopword` /
`RELATION_HOP1_WEIGHT` / `RELATION_HOP2_WEIGHT` 在本文件里已无人使用（随检索层归了模块自己
require）。**没裁** —— 等价搬迁不掺清理。已在原注释里写清，删不删她定。

**③ 牵挂闸比设计意图窄得多（真发现，**没改**）**。`getAttentionMaterial` 里两个闸的关系是：
提及闸用**原始** msg 做 `includes`，牵挂闸用的是 `extractNgrams` **去掉标点之后**的 n-gram。
「去标点后的串包含」严格弱于「原始串包含」，所以：**提及闸一个都没中时，牵挂闸能捞回来的，
只有「关键词被标点/空格从中间切开」那一种写法**（比如「关于搬 家」——提及闸漏，牵挂闸中）。
正常连着写的中文，牵挂闸永远捞不出提及闸漏掉的东西。也就是说这条闸的实际作用面
≈「她打字时在词中间插了空格/标点」。**这是设计意图（高牵挂线头 + 共享词 → 唤起）与实际
行为之间的落差**，不是 bug（代码照设计跑），但值得她知道：**悬着的线头不会因为「提到了
相关的事」而被想起来，除非提及闸已经命中**。想改语义要先跟她对。已有一条测试把这两种
写法钉在一起（`test/lib-context-retrieval.test.cjs` 的牵挂闸那条）。

**④ `isExactWord(msg, '')` 会死循环（**没改**，当前不可达）**。根因：`''.indexOf('', from)`
把 `from` 夹到 `str.length`，于是 `from = idx + 1` 永远推不过串长、永远等不到 `-1`。
唯一的调用点在 `retrieveWorld`，那里有 `if (!kl || !msg.includes(kl)) continue` 挡着，
所以线上不可达。**没有在重构里顺手改**（重构只许等价），但这是颗雷：谁哪天给
`isExactWord` 加一个新调用点、又没挡空串，就是一次挂死。`lib/context/select.js` 文件头和
`test/lib-context-retrieval` 的 spec 里都写了，spec 里**故意不放** `['猫','']` 这个向量
（放进去基线自己就挂死，这也正是发现它的方式）。

### 下一步

第 3 步还剩：`lib/context/` 的组装（`buildModelContext`，**动刀前先按 §2-新 补那条
「固定输入 → messages 结构稳定」的快照**）、`lib/context/session.js`、`lib/memory/`、`lib/wake/`。
方法照 §1 五步 + 上面三条升级，别再手工比对。

**别忘了**：`test/lib-context-select.test.cjs` 和 `test/lib-context-retrieval.test.cjs` 里的
基线**不许在重构里更新**。它红了 = 行为变了，先解释清楚再谈更新。

---

## §10 2026-09-10/11 续：分区第 3 步（第三片 `session` + 第四片 `build` 已落地）

第 3 步的心脏地带两片都搬完了。**未部署、未提交。** `npm test` **107/107**（含 84 条路由快照）。

| 片 | 内容 | 新文件 | server.js | 不变式 |
|---|---|---|---|---|
| 三 | 会话 IO 管子（config/state/segments/流水/配轮/分页历史 + 缓存锚点读写） | `lib/context/session.js`（238 行） | 7062 → 6891 | `test/fixtures/context-session.baseline.json`（**43 组**） |
| 四 | 核心组装 `buildModelContext` + `AQ_CONTRACT` + 天气状态 | `lib/context/build.js`（577 行） | 6891 → **6413** | `test/fixtures/context-build.baseline.json`（**48 组**） |

四组比对（每片各跑 `--rev` 与 `--module` 两条路）全部**逐字节一致**。
第 3 步到此只剩 `lib/memory/`（记忆编辑者 ≈577 行）和 `lib/wake/`（keepalive + 沉淀 ≈985 行）。

### 这一轮的方法升级：把「返回值和 IO 都测不到的东西」逼出来

第三片的向量每组比**四样**：`value`（返回值）/ `io`（假 supabase 的查询序列）/ `logs`（console.warn 逐句）/
`warns`（`warnConfigFallback` 的**组名与理由**）。第四样是这一片独有的 ——
这一片的全部价值就在「失败时退化成什么」，而退化是**不出声的**，只有 warn 参数分得清
「读到 no-row 走默认」和「读到 error 走默认」。`ctx_noRow → warns: []`、`ctx_error → warns: ["context:column live_max_tokens does not exist"]` 一红一绿，是两组不同的向量压出来的。

第四片的向量每组比 `value`（整个 messages 结构 + diagnostics）/ `io` / `logs`（尤其那条
`🧩 [动态注入]` 的块清单与丢弃清单）/ `warns`。**48 组**里专门有一条为「丢弃路径」而立
（见下 §坑①）。

### 这一轮的两个真决定（都是非逐字的，写在这里等她过目）

**① `warnConfigFallback` 不做这一片的边界内符号 —— 按依赖面判定，留在 server.js 注入。**
它字面上就挨着 `getContextConfig`（server.js:1993），看起来该随手带走。但它的调用点是
**六个配置组**：mirror / context / satisfy / memory_gate / keepalive / want_inject。
符号的归属由**依赖面**定，不由行的相邻定 —— 只带「context」那一处等于把它劈成两半。
所以它留在 server.js，跟 `warnOnce` 一样**注入**进来（先例：`lib/context/retrieval.js` 的工厂签名里
已经有 `warnConfigFallback` 一项）。spec 里给它一个 **echo 桩**（把组名+理由记进数组），
这样「降级要出哪一声」照压不误。
⚠️ 顺带避开一个 TDZ：检索层的工厂在 server.js:1971 就在消费 `warnConfigFallback`，
把它搬到 1971 行以下会直接炸模块加载。它是 `function` 声明（提升过），所以留在原处、
在下面实例化 session 工厂不会踩 TDZ。

**② `currentWeather` 的宿主换了 —— 绑定跟着状态走，不跟着调用走。**
`let currentWeather` 原本是 server.js 的模块态，三处碰它：`buildModelContext` 读、
GET `/api/location` 读、POST `/api/location` 写。搬迁时有两个选择：
(a) 按值注入给 build 工厂 —— **错的**。按值注入等于把那一眼天气**冻成快照**，
    POST `/api/location` 之后新天气进不去 build 的闭包，沈晏永远以为窗外还是上次那个天。
(b) 把 `let` 搬进 build 工厂闭包，另外开 `getWeather` / `setWeather` 两个口子 —— **采用的**。
`/api/location` 两个 handler 改成走这两个口子，并留了注释「别再在本文件里加 `currentWeather` 变量」。
这跟前一轮 `liveAnchors` 的处理是同一条规矩：**绑定必须跟着状态走，不能跟着调用走**。
区别是 `liveAnchors` 的 canonical 在 DB（`sessions.live_anchor_turn`），换宿主无痛；
`currentWeather` 的 canonical 只在内存里，所以必须把口子交出来。

### 踩到的坑

**① 为「丢弃路径」专门补了一条不带 attention 的同族向量 —— 原来那条压根走不到。**
`build_blocks_overflow_drop` 用来压「块数超上限 3 → 按优先级 slice(3) 丢块」那条路径。
搬完才发现它**从来没压到**：它在「世界书推入之前」就撞上那个 `attention is not defined`
的 ReferenceError 先炸了（见 §8 的真窟窿），排序与 slice 那几行根本没执行。
补的 `build_blocks_overflow_drop_noAttention`（`turns:2, gapMin:90`，不传 attention 素材）
一次凑齐 5 块 —— world(2) + residue(3) + weather(4) + calendar(4) + time(5) ——
上限 3 后**真的丢掉两块**（`dropped=residue,world(1)`），`slice(3).map` 那一支才第一次走全。
⚠️ 顺带一条经验：**一条向量红了不一定是搬错了，也可能是它本来就没在压它名字说的那件事。**
`gapMin:0` 时只丢 1 块，差点把这个坑放过去。

**② `server.js` 切片脚本第一版把文件写坏了。**
`[...L.slice(0,2710), ...rep, ...L.slice(2710)]` —— 保留了 1–2710 行**又**追加了替换段，
结果 `SyntaxError: Identifier 'buildModelContext' has already been declared`。
修法：先删原块再加新块，并且边界用**内容断言**（「第 2210 行是『核心组装』注释、第 2704 行是 `}`」）
不用裸下标。中途还吃了 `L[n-1]` 与「第 n 行」差一的亏，断言报 `2209 不是核心组装注释: ""` 才定位到。
⚠️ `node -e` 里写含 `^\/\*` 的正则会被 shell 吃掉转义（`re.test is not a function`），
**把脚本写成 `.tmp-*.cjs` 文件再跑**，别内联。

**③ `node --test` 里 `console.log` 没有输出**（node:test 按测试缓冲 stdout）。
调试要靠「把断言换成基线已证过的量」，或者写独立的 `node -e` 脚本对照。
这一轮有两条断言是我自己写错了、不是代码错：
(a) 心跳那条我断言 messages 里没有 `【当前时间】`，得到 `true` —— 独立脚本证明模块行为与基线一模一样，
    改成压基线证过的三样（`token_breakdown.dynamic === 0`、无 `🧩` 日志、`resume_gap_min === null`）。
(b) 写入面那条我预期 1 次 `sessions.update`，实际 **2 次**（`last_time_notice_at` + `live_anchor_turn`）——
    改成 2 并写明原因。**基线是裁判，我的直觉不是。**

### 下一步

第 3 步只剩两片：`lib/memory/`（记忆编辑者，≈577 行）、`lib/wake/`（keepalive + 沉淀，≈985 行）。
两片都消费 `warnConfigFallback`（调用点 3877 / 4858 / 4990），边界已定（注入，同 §10 ①）。
先按 §2-新 补不变式再动刀：memory 那片的行为一半在**写回的内容**上，
wake 那片的一半在**什么条件下才醒**上 —— 都还没建基线。

⚠️ 老规矩照旧：`context-session.baseline.json` / `context-build.baseline.json` **不许在重构里更新**。
它红了 = 行为变了，先解释清楚再谈更新。

---

## §11 2026-09-11 续：分区第 3 步（第五片 `memory` 已落地 + 一个真窟窿）

> 这一节是**给复查的人**看的。§11 的结论如果和 §9/§10 冲突，以本节为准。
> 独立的复查报告见 `docs/2026-09-11-review-for-opus.md`（那份是拿去审的，这份是留下当手册的）。

### 落地了什么

| 片 | 模块 | 行数 | 基线向量 | server.js |
|---|---|---|---|---|
| 三 `session` | `lib/context/session.js` | 238 | 43 | 7067 → 长 |
| 四 `build` | `lib/context/build.js` | 577 | 48 | → 6413 |
| **五 `memory`** | **`lib/memory/index.js`** | **571** | **108** | 6413 → **5903** |
| 五附带 | `lib/ui-markers.js` | 28 | 6 | —— |

`server.js` **7067 → 5903**（相对 HEAD `f7e49da`，那条 commit 里前两片已提交）。
测试 **107 → 121**（新增 `test/lib-memory.test.cjs` 14 条）。`require('./server.js')` 出 47 个导出，`node --check` 干净。

**五条基线、十条比对路径全部逐字节一致**（每片 `--rev` 与 `--module` 各一条）：

```
context-select   --rev 1f96a08  ✅    --module lib/context/select.js     ✅
context-retrieval --rev 2b9c8c5 ✅    --module lib/context/retrieval.js  ✅
context-session  --rev f7e49da  ✅    --module lib/context/session.js    ✅
context-build    --rev f7e49da  ✅    --module lib/context/build.js      ✅
memory-write     --rev f7e49da  ✅    --module lib/memory/index.js       ✅
```

⚠️ **select / retrieval 的基线是对着各自的 `baseRev` 写的**（`1f96a08` / `2b9c8c5`），
拿 `f7e49da` 去切会切错地方（工具会警告，但 `--module` 那一路照样绿 —— 别被半绿骗了）。

### 这一片压的是五样，其中两样是别处压不到的

返回值 / 表快照 / 查询序列 / **Ombre 与 DeepSeek 的调用参数** / 日志与告警。

**为什么非要多这两样**：这一片有一类错误**在返回值上完全看不出来**——
`hold` 的 `tags` 必须是 **string**（传数组就是 validation error，历史上 hold 因此**从来没成功过**）；
`trace` 的必填是 **`bucket_id`**（传 `id` 同样是 validation error）。
两处都是「调用发生了、返回值也没炸、OB 里啥也没有」。只有把「调用时到底传了什么」冻进基线才钉得住。
👉 这条经验可以推广：**凡是「静默失败」的外部契约，基线必须包括入参，不止返回值。**

### 两个边界决定（都是非逐字的，等人过目）

**① `stripUiMarkers` 没有跟着 memory 片走，而是单独搬去 `lib/ui-markers.js`。**

它原先**物理上坐在 memory 块正中间**（`gateMemoryWriteViaDeepSeek` 和 `scheduleMemoryWrite` 之间），
按「行的相邻」它该跟着 memory 走。但按**依赖面**：它零依赖（纯叶子），却被**三个域**在用 ——
备份导出（`server.js:1028`）、残留窗口（`server.js:2515`）、记忆门控（`lib/memory/index.js`）。

两条路都不对：
- **留在 server.js 注入** → spec 里得再抄一份实现，**两份副本漂移没有任何测试能发现**（这是最坏的）。
- **跟着 memory 走，别的域 require 它** → 三个域里有两个要跨模块去 require 一个跟它们无关的东西。

选的是第三条：**搬去 `lib/ui-markers.js`，三个域都 require 同一份**。零依赖的纯叶子适合当共享件。
⚠️ 有一个**新引入的初始化顺序风险**，写在 server.js 的接线注释里了：
它原来是**函数声明**（靠提升），现在是 **require 上来的 const**。
当前两个调用点都在函数体里、运行期才跑，没问题；但**模块顶层直接调它会踩 TDZ**。

**② 工厂交回 22 个内部函数，其中 21 个只有测试在用。**

`lib/memory/index.js` 的返回对象里，`scheduleMemoryWrite` 是 server.js 唯一真正消费的入口
（两条聊天后台链路各一处：`server.js:3264` / `server.js:5721`），
另外 21 个（`parseEventTime` / `normalizeMemoryWrite` / `holdNewMemory` / `traceUpdateMemory` …）
**纯粹是为了让 108 组向量能直接压每个函数**才交出去的。

这在「接口最小化」的意义上是脏的 —— 计划里 Opus 该骂就骂。我的理由是：这一片的行为一半藏在
「每一步的归一化」里（importance 钳位、key_facts 并集、grounding 过滤），
只从 `scheduleMemoryWrite` 的入口测，这些分支全被埋在两个网络调用后面，**根本进不去**。
返回对象里留了明确的分隔注释，标明哪些是接口、哪些只为测试；要收窄的话，
正确做法是**同时**把 spec 改成只走入口，而不是随手删导出。

### 状态搬进闭包：两个 Set

`memoryWriteLocks`（单实例内存锁）和 `memoryWriteProcessed`（本进程已处理窗口哈希）
跟着代码进了工厂闭包。**没有任何外部调用方碰过它们**（连裸引用都没有，
`grep -E 'memoryWriteLocks|memoryWriteProcessed' server.js` 为空），
所以这次是**纯闭包迁移**，不像 `currentWeather` 那样需要开访问器 —— 那个的 canonical 在内存里且有外部写入方。
每次 `createMemory()` 拿到的是**独立**的两个 Set（测试因此互不串味，`test/lib-memory.test.cjs` 每个用例一副新库）。

### ⚠️ 这一轮的真发现：`getAllMemoryTopics` 的 fail-closed 是**假的**（只查不修，交人判断）

`lib/memory/index.js:314`：

```js
async function getAllMemoryTopics() {
  try {
    const { data } = await supabase.from('memory_topics').select('*');
    return data || [];          // ← 只解构 data
  } catch (e) {
    console.error('💥 读取 memory_topics 失败（本轮差分写回将跳过）:', e.message);
    return null;                // ← 注释说「fail-closed：读失败返回 null」
  }
}
```

调用方 `generateMemoryWriteIfNeeded`（`index.js:300`）靠 `if (topics === null) return;` 挡写回。
**但 supabase-js 的失败不是 throw，是 `{ data: null, error: {...} }`。** 它不抛，
所以走的是 `return data || []` → 回的是 **`[]`**，不是 `null`。

**后果**：看不到任何旧主题 → 每个主题都被当成全新的 → 全部 `hold` → **Ombre 重复建桶**。
这正是那段注释自己说要防的「永久污染」。也就是说：
**这道闸对最常见的失败形态（权限错、列不存在、网络抖动返回 error）根本不响**，只有真 throw（如假 supabase 的 `__throw`）才响。

**没有修**（搬迁纪律：等价搬迁不掺修复）。但**基线把这落差原样压住了**，
而且是**成对**压的 —— 这两条向量的输出**故意不一样**，谁"顺手修好"了它，测试会红：

| 向量 | 表读失败的方式 | 结果 |
|---|---|---|
| `wm_topicsThrow` | **真抛** | 不发 hold、`degraded: ["memory_topics_read_failed"]`、两条明确日志 ← 闸响了 |
| `wm_topicsErrorFieldProceeds` | 回 `error` 字段 | **照样 hold 了一个新桶并 upsert**、`degraded: []`（**静默**，没有任何"降级"标记）← 闸哑了 |
| `gm_topicsThrowCloses` | 真抛 | 整轮跳过 |
| `gm_topicsErrorFieldProceeds` | 回 `error` 字段 | 整轮继续，写回发生 |

`test/lib-memory.test.cjs` 里也有一条独立的白话断言钉着这件事（"fail-closed 的缺口…"）。
**修法**（供她/复查者拍板，我没动）：`const { data, error } = await …; if (error) return null;` —— 一行。

### 基线顺手压住的几条反直觉行为（都不是 bug，是现状，别再"顺手修"）

- **`importance: 0` 会被顶成 `0.5`**（`Math.min(Math.max(parseFloat(x) || 0.5, 0), 1)`）；
  **负数**才真的被夹到 `0`。也就是说「模型明确说这条不重要」和「模型没说」落库是同一个值。
- **`key_facts` 过滤映射把非字符串变成字符串**：数字 `42` → `"42"`，`null` → `"null"`（两者都被保留）。
- **`getMemoryGateConfig` 的 `catch` 分支不调 `warnConfigFallback`**，
  而 `error` / `noRow` 两个分支调 —— 三条路的退化痕迹**不是对称**的（`gc_error` / `gc_noRow` / `gc_throw` 三组向量分别压住）。
- **判官说 `false` 时连主题表都不读**（`gm_gateSaysNo` 的 IO 里没有 `from:memory_topics`）——
  这就是判官存在的全部价值：省掉「全表 topic 读 + 30 条列表的大 prompt」。

### 踩到的坑

**① 一行删行审计：证明「没有顺手多删」不能靠行数算术。**
行数对不上（模块加起来 1414 行，server.js 只少了 1226 行）时，唯一靠谱的证法是
**把 `git diff -U0` 里被删的每一行拿去新模块里逐字找**。结果是：1226 行被删，
**其中 1223 行逐字出现在四个新模块里**，剩下 3 行正是 §10 那两个 `currentWeather` → `getWeather()/setWeather()`
的口子替换（有据可查）。剩下的差额全在模块新增的头注释 / `module.exports` 脚手架里。
👉 **这条应当固化成工具**（`.tmp-diffaudit.cjs` 用完删了）—— 它比行数算术强得多，下次搬大块先跑它。

**② 我写错了三条语义断言，基线才是裁判（第三次了）。**
(a) `importance` 那条我传的 content 是「事实一 / 事实二」——**3 个字**，被 `content.length >= 4` 全过滤掉了，
拿到 `[]` 才发现断言测的根本不是钳位；
(b) `refineFeelContent` 那条我把桩的返回值设在 `run()` **外面**，而 `run()` 开头的清理会把它重置 —— 拿到 `null`；
(c) 窗口去重那条我把判官桩写成了主分类的返回形状（`{"should_write":false}` 里含 `false`，
被 `normalizeGateResult` 匹配走，判官直接说「不值得」），于是**主题表一次都没读**，`0 !== 1`。
👉 三条都不是代码错。**「断言红了先怀疑断言」**，尤其是我现写的语义断言 —— 它们没有基线背书。

**③ `node --test` 的 stdout 缓冲**（§10 坑 ③ 又踩一次）：调试只能靠独立脚本或「把断言换成基线证过的量」。

### 下一步

**第 3 步只剩最后一片**：`lib/wake/`（keepalive + reflection，≈985 行）。
它消费 `warnConfigFallback`。
⚠️ 行号跟着 memory 片搬走挪过了，**搬 wake 之前必须重数一遍**。当前（`server.js` 5903 行版）全文件的调用点是：
`mirror` 831/836、`satisfy` 2621/2628、**`keepalive` 3692/3703**、**`want_inject` 3824/3830**；
`context` 与 `memory_gate` 两组已经跟着各自的片进了模块（注入进来的），server.js 里不再有。
边界已定（注入，同 §10 ①）——wake 那片预计吃掉后两组里的一部分。
⚠️ 动刀前先补基线：**这一片的行为一半在「什么条件下才醒」上**（时间窗、冷却、开关、静默判定），
而那套判定全是"不满足就什么都不做"，返回值一律是 `undefined` —— 按 memory 片的经验，
**得把「有没有醒 / 醒了几次 / 调了哪个模型」也冻进基线**，否则整片是隐形的。

⚠️ 纪律照旧：**五条基线一条都不许在重构里更新**。红了 = 行为变了，先解释清楚再谈更新。
⚠️ 本次改动**未提交、未部署**（`git status`：`server.js` / `package.json` / `scripts/audit/testkit.cjs` 已改，
`lib/context/{session,build}.js`、`lib/memory/`、`lib/ui-markers.js`、三个 spec、三个基线、两个测试文件未跟踪）。
